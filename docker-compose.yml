services:
  postgres:
    image: postgres:16
    container_name: aftersales-pg
    environment:
      POSTGRES_USER: aftersales
      POSTGRES_PASSWORD: aftersales
      POSTGRES_DB: aftersales
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aftersales"]
      interval: 5s
      timeout: 3s
      retries: 10
